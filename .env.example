# Copie para .env.local e preencha com as chaves do projeto Supabase do miso4slope.
# Sem essas variáveis, o app funciona normalmente (motor de cálculo é 100%
# client-side) — só o salvamento em nuvem fica indisponível, usando apenas
# o localStorage do navegador.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
